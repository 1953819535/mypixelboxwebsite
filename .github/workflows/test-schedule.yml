name: Cron Trigger Test

on:
  schedule:
    # 每15分钟运行一次，这是为了能快速看到结果
    - cron: '*/15 * * * *'
  workflow_dispatch:

jobs:
  health_check:
    runs-on: ubuntu-latest
    steps:
      - name: Echo current time
        run: echo "Cron test job executed at $(date)"